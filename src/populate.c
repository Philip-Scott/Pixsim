#include <stdio.h>
#include "pixsim.h"

//Generate light matrix
//TODO: Randomize colors
//TODO: Save "steps" to a stack
int populate () {
	int i,j;
	int a = 1;
	for(i = 0; i < x; i++) { // X Axis
	for (j = 0; j < y; j++) { // Y Axis
		array = a++;
	}}  
}
